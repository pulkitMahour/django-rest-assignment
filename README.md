# Simple drf assignment

This is a simple drf assignment, in this assignment a user perform CRUD actions. \
In this assignment a user can add,update,delete a cuboid with (length, width, height) dimensions. 